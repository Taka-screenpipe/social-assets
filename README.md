# social-assets

Public host for screenpipe social creative. Buffer's API requires a publicly
fetchable image URL, so finished cards land here and are referenced by their raw URL.

Design source and the render pipeline live in the marketing working directory
(`social-assets/BRAND.md`). Nothing here is authored by hand.
